<%= render 'menus/menu' %>

<div class="page">
  <h1 class="title"><%= @page.title %></h1>

  <% @blocks.each do |block| %>
  <%= render block %>
  <% end %>
</div>

<%= render 'menus/footer-menu' %>
