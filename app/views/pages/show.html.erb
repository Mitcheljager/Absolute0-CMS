<%= render 'menus/menu' %>

<div class="page">
  <h1><%= @page.title %></h1>

  <% @blocks.where(:page_id => @page.id).each do |block| %>
  <%= render block %>
  <% end %>
</div>

<%= render 'menus/footer-menu' %>
