<%= render 'layouts/theme/menu' %>

<div class="page">
  <h1 class="title"><%= @page.title %></h1>

  <% @blocks.where(:page_id => @page.id).order(:priority => 'asc').each do |block| %>
  <%= render block %>
  <% end %>
</div>

<%= render 'layouts/theme/footer-menu' %>
